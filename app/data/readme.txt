please create a file called key.ts in this directory, generate an apikey from https://account.mapbox.com/ and paste it into the API_KEY placeholder below,
then copy it and paste it into the newly created file called

export const apiKey = 'API_KEY';
